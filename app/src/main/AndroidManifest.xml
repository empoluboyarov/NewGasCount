<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.evgeniy.gascount">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenuActivity"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_2level.DewPointActivity"
            android:label="@string/title_activity_dewpoint"/>
        <activity android:name=".activity_2level.ConverterActivity"
            android:label="@string/title_activity_converter"/>
        <activity android:name=".activity_2level.CountsActivity"
            android:label="@string/title_activity_counts"/>
        <activity android:name=".activity_2level.MethodicsActivity"
            android:label="@string/title_activity_methodics"/>
    </application>

</manifest>
